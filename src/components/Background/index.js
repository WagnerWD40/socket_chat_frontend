import React from 'react';
import './styles.css';

export default function Background({ children }) {
    return (
        <div className="Background">
            { children }
        </div>
    );
};
